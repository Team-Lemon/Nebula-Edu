<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Welcome to Nebula-Edu</title>
</head>
<body>
    <!-- Header Area -->
    <header id="#" class="header-bar">
        <a href="/"><h1>Nebula-Edu.</h1></a>
    </header>

    <div class="content-container">
        <!-- Nav-Bar Area -->
        <nav class="nav-wrapper">
            <div class="nav-container">
                {{!-- Logout Area only displays if User is loggedIn --}}
                {{#if loggedIn}}
                <a href="/logout" id="logout">
                    <div class="nav-item">
                        <p class="nav-text">Logout Here</p>
                    </div>
                </a>
                {{else}}
                <a href="/login" id="login-btn">
                    <div class="nav-item">
                        <p class="nav-text">Login Here!</p>
                    </div>
                </a>
                {{/if}}
                <a href="/html-lessons" id="nav-btn">
                    <div class="nav-item">
                        <p class="nav-text">HTML</p>
                    </div>
                </a>
                <a href="css-lessons" id="nav-btn">
                    <div class="nav-item">
                        <p class="nav-text">CSS</p>
                    </div>
                </a>
                <a href="js-lessons" id="nav-btn">
                    <div class="nav-item">
                        <p class="nav-text">JavaScript</p>
                    </div>
                </a>
                {{#if loggedIn}}
                    <a href="/new-lesson" id="add-btn">
                        <div class="nav-item">
                            <p class="nav-text">Contribute!</p>
                        </div>
                    </a>
                    <a href="/dashboard" id="add-btn">
                        <div class="nav-item">
                            <p class="nav-text">Your DashBoard</p>
                        </div>
                    </a>
                {{/if}}
            </div>
        </nav>

        {{!-- Main Body/Content Area --}}
        <main>
            {{{ body }}}
        </main>
    </div>

    {{!-- Script Area, but only when logged in --}}
    {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>